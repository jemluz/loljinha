import Vue from 'vue'
import Router from 'vue-router'

Vue.use(Router)

// const routes = [{}]
// ou

export default new Router({
  mode: 'history',
  routes: [
    // {
    //   path: '/',
    //   name: 'home',
    //   component: Home
    // }
  ]
})
